import { ProfilePageClient } from "./profile-page-client";

export const metadata = {
  title: "Profile — Ghent Parking",
};

export default function ProfilePage() {
  return <ProfilePageClient />;
}
